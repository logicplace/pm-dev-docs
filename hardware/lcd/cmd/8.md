# ADC Select

* To write columns left to right, use:
  * libpmdd: `LCD_ADC(NORMAL);`
  * C raw: `LCD_CTRL = 0xA0;`
  * ASM: `LD LCD_CTRL, #0A0h`
* To write columns right to left, use:
  * libpmdd: `LCD_ADC(REVERSE);`
  * C raw: `LCD_CTRL = 0xA1;`
  * ASM: `LD LCD_CTRL, #0A1h`

TODO
